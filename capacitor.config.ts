import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '4angelsmobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
